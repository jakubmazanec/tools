#
# DO NOT EDIT!
# This file was autogenerated by Carson.
# Changes may cause incorrect behavior and will be lost when the file is regenerated.
#
# Run `npx carson update workspace` to regenerate.
#

name: Update pull request

on:
  pull_request_target:
    branches: [development]

jobs:
  update_pull_request:
    name: Update pull request
    if: ${{ github.actor == 'renovate[bot]' && contains(github.head_ref, 'renovate/') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.PAT }}
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.email "bot@mazanec.dev"
          git config user.name "Bot"

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install packages
        run: npm ci

      - name: Generate changeset
        run: npx tsx tools/scripts/create-dependencies-changeset.mts

      - name: Update Carson
        run: |
          npm install

      - name: Commit and push changes
        run: |
          git add . --all
          git commit -m"Update"
          git push --force
