---
'@jakubmazanec/carson-templates': minor
---

New script `create-templates-changeset.mts` for creating changeset was added; it's intended to be
run manually when updating dependencies that are handled by this package. Script for creating
changeset when updating dependencies was also slightly updated so the two are aligned.
