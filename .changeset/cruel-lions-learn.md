---
'@jakubmazanec/carson-templates': minor
---

Allow specifying custom TypeScript and Turborepo configs (using `overrides.tsconfig` or
`overrides.turbo` workspace option) in the `workspace` template.
