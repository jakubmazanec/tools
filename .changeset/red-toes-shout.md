---
'@jakubmazanec/ui': minor
---

Updated styles of components `Button`, `Checkbox`, `Combobox`, `DialogPanel`, `DialogTitle`, `Icon`,
`Input`, `Legend`, `ListboxOption`, `Radio`, `Spinner`, `TableHeader`, `TableHeader` and `Textarea`.
Components now also may receive `data-slot` attribute when necessary (e.g. when `Icon` is a direct
child of `Button`).
