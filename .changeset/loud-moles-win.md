---
'@jakubmazanec/carson-templates': minor
---

Allow specifying custom ESLint and Prettier configs (using `overrides.eslintConfig` or
`overrides.prettierConfig` workspace or project option) in all templates.
