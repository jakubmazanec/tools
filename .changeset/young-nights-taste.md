---
'@jakubmazanec/carson-templates': minor
---

Script that generates changesets for packages that had their dependencies changed is now located in
`tools/scripts` directory, so it can be called manually.
