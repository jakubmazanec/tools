---
to: 'source/tailwindConfig.ts'
strategy: create
---
import {type Config} from 'tailwindcss';

export const tailwindConfig = {
  content: [<%- JSON.stringify(`./${SOURCE_DIRECTORY}/**/*.{js,jsx,ts,tsx}`) %>],
  theme: {
    extend: {},
  },
  plugins: [],
} satisfies Config;
