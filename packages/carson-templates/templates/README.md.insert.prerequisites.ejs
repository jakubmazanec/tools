---
to: 'README.md'
strategy: [insert, prerequisites]
---

#### Prerequisites

- Node.js <%- semver.major(semver.minVersion(DEPENDENCY_VERSIONS.node)) %> or later
- TypeScript <%- semver.major(semver.minVersion(DEPENDENCY_VERSIONS.typescript)) %> or later
